// Tiny smoke fixture — confirms even the smallest TSX still resolves React/JSX
// runtime entries deterministically and survives a hash-stability probe.
export default function Empty() {
  return null;
}
